# This is a multi-stage build, so we need to specify the FROM directive twice.
# The first stage is called builder and the second stage is called production.
# We first use the node alpine image to build the app and then use nginx to serve it.

FROM node:16-alpine@sha256:fcb03294d3c0695cf9762dec94c0366f08e7a8c6a3c1e062d38c80ac30684d8a as builder

WORKDIR /app
COPY client .
RUN npm ci 
RUN npm run build

FROM nginx:1.21.0-alpine@sha256:cc8c413c74aba9fef9dae7f3da736725136bad1e3f24fbc93788aea1944f51c4 as production

ENV NODE_ENV production
COPY --from=builder /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]